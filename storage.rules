rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{ownerId}/{fileId} {
      allow read: if false;
      allow write: if request.auth != null && request.auth.uid == ownerId;
      allow delete: if request.auth != null && request.auth.uid == ownerId;
    }
  }
}
